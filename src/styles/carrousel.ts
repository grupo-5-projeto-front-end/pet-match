import styled from "styled-components";

export const StyledCarrousel = styled.ul `
    height: 400px;
    width: 100%;

    display: flex;
    align-items: center;
    gap: 15px;

    overflow-x: auto;
`