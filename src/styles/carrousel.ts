import styled from "styled-components";

export const StyledCarrousel = styled.ul `
    height: 400px;
    width: 100%;

    max-width: 700px;

    display: flex;
    align-items: center;
    gap: 15px;

    overflow-x: auto;
`