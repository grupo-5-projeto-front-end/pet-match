export interface iStyledLinkProps {
    width: string;
    height: string;
    background: string;
    fontSize: string;
    color: string;
  }

export interface iStyledMenuProps {
    width: number;
}